# 현재 나이트가 위치한 곳의 좌표
place = input()
row = int(place[1])
column = int(ord(place[0])) - int(ord('a')) + 1

# 나이트가 움직일 수 있는 범위
steps = [(-2,1),(-2,-1),(2,1),(2,-1),(-1,-2),(1,-2),(-1,2),(1,-2)]

# 나이트가 움직일 수 있는 경우의 수 저장변수
count = 0

# 나이트가 움직일 수 있는 경우 확인
for step in steps:
    nx = column + step[0]
    ny = row + step[1]
    
    # 나이트가 이동할 수 없는 경우 확인
    if nx < 1 or nx > 8 or ny < 1 or ny > 8:
        continue
    count += 1
# 결과 출력    
print(count)
