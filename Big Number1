n,m,k = map(int, input().split()) # 배열의 크기 n, 숫자가 더해지는 횟수 m, 연속해서 더해질수 있는 횟수 k (공백 간격두고 입력받음)
data = list(map(int, input().split())) # 입력받을 데이터 리스트 (공백 간격두고 입력받음)

data.sort() # 입력받은 데이터 정렬
fisrt = data[n-1] # 입력받은 데이터 중 가장 큰 수
second = data[n-2] # 입력받은 데이터 중 두 번째로 큰 수

result = 0 # 큰 수의 법칙 적용한 결과를 담을 변수 0으로 초기화

while True:
    for i in range(k): # 가장 큰 수를 K번 더하기
        if m == 0:
            break
        result += fisrt
        m -= 1
        break
    if m == 0:
        break
    result += second # 두 번째로 큰 수를 한 번 더하기
    m -= 1
   
print(result)
        
