# 정수 입력
n = int(input())
# 결과 변수 초기화
count = 0

# 00시 00분 00초 ~ N시 59분 59초 사이에 '3'이 들어가는 시각 횟수 세는 3중 반복문
for i in range(n+1):
    for j in range(60):
        for k in range(60):
            time = str(i)+str(j)+str(k)
            if '3' in time: # 매 시각 안에 '3'이 포함되어 있다면 count 증가
                count += 1
                
print(count)
